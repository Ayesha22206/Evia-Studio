namespace Evia.Web.Services
{
    //public record AiDesignRequest(string Style, string Fabric, string Color, string Pattern, string Size);

    //public record AiDesignResult(bool Success, string? ImageUrl, string? ErrorMessage);

    //// Abstraction so the AI image provider can be swapped (OpenAI, Stability AI,
    //// Replicate, Azure OpenAI, a self-hosted Stable Diffusion endpoint, etc.)
    //// without touching DesignController or the Razor view.
    //public interface IAiDesignService
    //{
    //    Task<AiDesignResult> GenerateOutfitImageAsync(AiDesignRequest request, CancellationToken ct = default);
    //}
}
